name: mlflow-project-example

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      num_trees: {type: int, default: 20}
      max_depth: {type: int, default: 5}
    command: "python train.py --num_trees {num_trees} --max_depth {max_depth}"